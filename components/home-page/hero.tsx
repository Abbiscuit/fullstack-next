import React from 'react';

import classes from './hero.module.css';

interface Props {}

const Hero = (props: Props) => {
  return (
    <div>
      <h1>Hero</h1>
    </div>
  );
};

export default Hero;
